syntax = "proto3";

package example.simple;

option go_package = "simplepb"; // this is used to generate the package name

message Simple {
  int32 id = 1;
  bool isSimple = 2;
  string name = 3;
  repeated int32 sample_list = 4;
}